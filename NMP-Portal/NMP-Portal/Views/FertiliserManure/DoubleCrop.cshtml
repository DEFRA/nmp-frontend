@using NMP.Commons.Resources
@model NMP.Commons.ViewModels.FertiliserManureViewModel
@{
    string title = (!string.IsNullOrEmpty(Model.EncryptedFertId)) ? Resource.lblUpdateInorganicFeriliser : Resource.lblAddInorganicFeriliser;
    ViewData["Title"] = $"{string.Format(Resource.lblWhichCropIsThisFertiliserApplication, Model.FieldName)} {Resource.lblHyphen} {title}";
    
    ViewBag.CurrentStep = (int)NMP.Commons.Enums.FertiliserWizardSteps.DoubleCrop;
}
@section BeforeContent {
    @if(Model.IsCheckAnswer&&(!Model.IsAnyChangeInField))
    {
        <govuk-back-link asp-action="CheckAnswer" asp-controller="FertiliserManure">@Resource.lblBack</govuk-back-link>
    }
    else{
    <govuk-back-link asp-action="DoubleCrop" asp-controller="FertiliserManure" asp-route-q="@Model.DoubleCropEncryptedCounter">@Resource.lblBack</govuk-back-link>
    }

}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <form asp-action="DoubleCrop" asp-controller="FertiliserManure" method="post" autocomplete="off">
            @Html.AntiForgeryToken()
            @if (TempData["DoubleCropError"] != null)
            {
                <govuk-error-summary>
                    <govuk-error-summary-item>@TempData["DoubleCropError"]</govuk-error-summary-item>
                </govuk-error-summary>
            }
            @await Html.PartialAsync("~/Views/FertiliserManure/_FertiliserWizardHiddenFields.cshtml", Model)
            @if (Model.DoubleCrop != null && Model.DoubleCrop.Count > 0)
            {
                @for (int i = 0; i < Model.DoubleCrop.Count; i++)
                {
                    @if (Model.DoubleCropCurrentCounter == i)
                    {
                        <govuk-radios for="@Model.DoubleCrop[i].CropID">
                            <govuk-radios-fieldset>
                                <govuk-radios-fieldset-legend is-page-heading="true" class="govuk-fieldset__legend--l">
                                    @string.Format(Resource.lblWhichCropIsThisFertiliserApplication, Model.FieldName)
                                </govuk-radios-fieldset-legend>
                                @if (ViewBag.DoubleCropOptions != null)
                                {
                                    @foreach (var item in ViewBag.DoubleCropOptions)
                                    {
                                        <govuk-radios-item value="@item.Value">@item.Text</govuk-radios-item>
                                    }

                                }

                            </govuk-radios-fieldset>
                        </govuk-radios>
                    }
                        else
                        {
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.DoubleCrop[i].CropID" />
                        }
                    }
            }
            <govuk-button type="submit" class="margin-bottom-forty">
                @Resource.lblContinue
            </govuk-button>
            <p class="govuk-body cancel-link">
                @if (string.IsNullOrWhiteSpace(Model.EncryptedFertId) && (!Model.IsCheckAnswer))
                {
                    <a asp-action="CreateFertiliserManureCancel" asp-controller="FertiliserManure" asp-route-q="@Model.EncryptedFarmId" asp-route-r="@Model.EncryptedHarvestYear">@Resource.lblCancel</a>
                }
                else
                {
                    <a asp-action="CheckAnswer" asp-controller="FertiliserManure">@Resource.lblCancel</a>
                }
            </p>

        </form>
    </div>

</div>

